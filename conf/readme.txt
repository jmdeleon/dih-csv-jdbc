Adjust the path in db-data-config.xml to read from the csv directory.

This sample core should otherwise be already configured to run as a core in a Solr 4.x installation.

The core files needed are solrconfig.xml, schema.xml and db-data-config.xml
